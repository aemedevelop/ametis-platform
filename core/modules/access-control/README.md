# access-control

Responsibilities:

- Manage roles and permissions.
- Evaluate authorization checks using role-permission mappings.
- Support vertical permissions (`mercantil.*`, `comercial.*`, `financiera.*`, `ia.*`).
