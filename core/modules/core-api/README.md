# core-api

Responsibilities:

- Expose REST APIs for Core domains.
- Validate auth context (`user_id`, `tenant_id`).
- Delegate business rules to domain modules.
- Return consistent error model and audit metadata.
